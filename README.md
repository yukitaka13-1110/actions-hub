# py-utils

日常的に使う小さな Python ユーティリティスクリプト集。

## Scripts

### csv2json

CSV ファイルを JSON に変換する。

```bash
python csv2json.py input.csv -o output.json
```

### file_organizer

指定ディレクトリ内のファイルを拡張子ごとにサブディレクトリに整理する。

```bash
python file_organizer.py ~/Downloads
```

## Requirements

- Python 3.12+
